apiVersion: 1

providers:
  - name: "My-dashboards"
    orgId: 1
    # Grafana watches this folder and auto-loads any .json dashboard files
    folder: "Tracer"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/dashboards
